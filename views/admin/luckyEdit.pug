extends ../layout

block content

    br
    div.container
        div.row
            div.form.form-inline
                a.btn.btn-lg(href='/admin/lucky?course-id='+course_id+'&course-title='+course_title+'&user-id='+user_id)
                    i.fa.fa-chevron-left
                h2='Editing ' + course_title + ': Lucky Bonus ' + id
    hr

    div.container
        div.row
            form(method='POST' action='' onsubmit="return confirm('Do you really want to submit your changes?');")
                input(type='hidden', value=course_id, name='course-id')
                input(type='hidden', value=course_title, name='course-title')
                input(type='hidden', value=user_id, name='user-id')

                div.form-group.form-inline
                    label(for='badge_points') Date and Time of Lucky Bonus:
                    input#badge_points.form-control(type='datetime-local', name='date_time')

                    label(for='point_value') Point value of Lucky Bulldog:
                    input#point_value.form-control(type='numeric', value=lucky_bonus.point_value, name='point_value')

                    label(for='image_name') Image to display:
                    input#image_name.form-control(type='numeric', value=lucky_bonus.image_name, name='image_name')

                hr

                button.btn.btn-success(type='submit') Save Changes
